spring:
  datasource:
    url: jdbc:postgresql://localhost:5001/evote_system
    username: evote
    password: evote_pass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update